{
  "installCommand": "yarn install",
  "buildCommand": "yarn --cwd packages/workflow build && yarn --cwd plateform_front build",
  "outputDirectory": "plateform_front/build",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
